# PendingPromise source package

The main Beyond module exports a native Promise subclass with externally accessible settlement functions. Use `@beyond-js/pending-promise/main` and await the instance directly.

Read the repository [overview](../README.md) and [architecture/compatibility guide](../docs/architecture.md) for construction semantics, the absent value getter and build/test prerequisites.
